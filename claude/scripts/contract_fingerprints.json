{
  "analyze_sessions.py": [
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --verbose/-v: ",
    "arg cost --by: \u2014 rollup grouping: total, day, project, harness, model, session (default: total) (choices: total, day, project, harness, model, session; default: total)",
    "arg cost --cwd: \u2014 filter by working directory substring",
    "arg cost --grep/-g: \u2014 filter message text by substring or regex",
    "arg cost --harness: \u2014 harness to analyze: all, pi, claude, opencode, copilot, agy (default: all; note: agy is opt-in and excluded from 'all') (choices: all, pi, claude, opencode, copilot, agy; default: all)",
    "arg cost --json: \u2014 emit output as JSON",
    "arg cost --limit: \u2014 limit number of results",
    "arg cost --model: \u2014 filter by model name substring",
    "arg cost --no-subagents: \u2014 exclude subagent / child sessions (included by default)",
    "arg cost --quiet/-q: ",
    "arg cost --session: \u2014 filter by session ID substring",
    "arg cost --since: \u2014 filter sessions on/after date/time (ISO 8601 YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS, or relative e.g. 7d, today)",
    "arg cost --until: \u2014 filter sessions on/before date/time (ISO 8601 YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS)",
    "arg cost --verbose/-v: ",
    "arg prompts --cwd: \u2014 filter by working directory substring",
    "arg prompts --format: \u2014 output format (default: markdown) (choices: markdown, jsonl; default: markdown)",
    "arg prompts --grep/-g: \u2014 filter message text by substring or regex",
    "arg prompts --harness: \u2014 harness to analyze: all, pi, claude, opencode, copilot, agy (default: all; note: agy is opt-in and excluded from 'all') (choices: all, pi, claude, opencode, copilot, agy; default: all)",
    "arg prompts --include-subagents: \u2014 include subagent / child sessions (excluded by default)",
    "arg prompts --json: \u2014 emit output as JSON",
    "arg prompts --limit: \u2014 limit number of results",
    "arg prompts --model: \u2014 filter by model name substring",
    "arg prompts --quiet/-q: ",
    "arg prompts --session: \u2014 filter by session ID substring",
    "arg prompts --since: \u2014 filter sessions on/after date/time (ISO 8601 YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS, or relative e.g. 7d, today)",
    "arg prompts --until: \u2014 filter sessions on/before date/time (ISO 8601 YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS)",
    "arg prompts --verbose/-v: ",
    "arg search --context/-C: \u2014 context lines around match (default: 0) (default: 0)",
    "arg search --cwd: \u2014 filter by working directory substring",
    "arg search --grep/-g: \u2014 filter message text by substring or regex",
    "arg search --harness: \u2014 harness to analyze: all, pi, claude, opencode, copilot, agy (default: all; note: agy is opt-in and excluded from 'all') (choices: all, pi, claude, opencode, copilot, agy; default: all)",
    "arg search --include-subagents: \u2014 include subagent / child sessions (excluded by default)",
    "arg search --json: \u2014 emit output as JSON",
    "arg search --limit: \u2014 limit number of results",
    "arg search --model: \u2014 filter by model name substring",
    "arg search --quiet/-q: ",
    "arg search --regex/-r: \u2014 treat query as regular expression",
    "arg search --session: \u2014 filter by session ID substring",
    "arg search --since: \u2014 filter sessions on/after date/time (ISO 8601 YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS, or relative e.g. 7d, today)",
    "arg search --until: \u2014 filter sessions on/before date/time (ISO 8601 YYYY-MM-DD or YYYY-MM-DDTHH:MM:SS)",
    "arg search --verbose/-v: ",
    "arg search query: \u2014 search query text (nargs: ?)",
    "exit codes: ",
    "handler cost: ",
    "handler prompts: ",
    "handler search: ",
    "subcommand cost: token and USD cost analysis and rollups",
    "subcommand prompts: list user prompts",
    "subcommand search: search message transcripts"
  ],
  "dev_status.py": [
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --verbose/-v: ",
    "arg add json: ",
    "arg approve --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg approve id: ",
    "arg backfill-gate --apply: \u2014 write changes (default: dry run)",
    "arg block --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg block blocker: ",
    "arg block id: ",
    "arg done --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg done id: ",
    "arg gate-pass --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg gate-pass id: ",
    "arg gate-pass json: \u2014 per-criterion coverage: run evidence or a manual note per criterion (nargs: ?)",
    "arg gate-set --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg gate-set id: ",
    "arg gate-set json: ",
    "arg list --raw: \u2014 machine-readable TSV (id\\tstatus\\tsummary) instead of the table",
    "arg list --status: \u2014 only show items with this status (choices computed at runtime)",
    "arg out-of-scope add --reason-file: (required)",
    "arg out-of-scope add --related-item: ",
    "arg out-of-scope add concept_slug: ",
    "arg out-of-scope link backlog_slug: ",
    "arg out-of-scope link concept_slug: ",
    "arg out-of-scope remove concept_slug: ",
    "arg out-of-scope show concept_slug: ",
    "arg out-of-scope unlink backlog_slug: ",
    "arg out-of-scope unlink concept_slug: ",
    "arg pending add json: ",
    "arg pending update --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg pending update id: ",
    "arg pending update patch: ",
    "arg prune --force: \u2014 required to prevent accidental prune (required)",
    "arg ready --prefix: \u2014 only items whose slug starts with this (e.g. meta-)",
    "arg recap --backend: \u2014 force this backend instead of priority-order fallback (choices computed at runtime)",
    "arg recap --refresh: \u2014 bypass the freshness cache and regenerate the recap now",
    "arg reject --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg reject feedback: ",
    "arg reject id: ",
    "arg remove --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg remove id: ",
    "arg rename --if-rev: \u2014 required when <old_slug> is numeric; get the current value from render/list/show immediately before this call",
    "arg rename new_slug: ",
    "arg rename old_slug: ",
    "arg review --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg review id: ",
    "arg run --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg run --timeout: \u2014 kill the command after this many seconds (default: 1800) (default: 1800.0)",
    "arg run command: \u2014 command to execute and record (everything after --; no shell) (nargs: *)",
    "arg run id: ",
    "arg runs id: ",
    "arg show id: ",
    "arg start --allow-main/--no-worktree-check: \u2014 allow starting item from the main/master repository checkout",
    "arg start --claimed-by: \u2014 override claimed harness/session identifier",
    "arg start --force/-f: \u2014 force start even if item is actively claimed by another session",
    "arg start --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg start id: ",
    "arg unblock --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg unblock blocker: ",
    "arg unblock id: ",
    "arg update --if-rev: \u2014 required when <id> is numeric; get the current value from render/list/show immediately before this call",
    "arg update id: ",
    "arg update patch: ",
    "exit codes: 1",
    "handler add: Handle ``add``: append a new backlog item. ``args.json`` must decode to an object with at least ``id`` and ``summary``; see :data:`BacklogItem` for the full field set.",
    "handler approve: Handle ``approve``: accept an in-review item, marking it done.",
    "handler backfill-gate: Handle ``backfill-gate``: stamp an explicit inert gate on legacy items. Items created before ``gate`` existed have no ``gate`` key at all \u2014 code already treats that as inert (see :func:`_gate_blocks`). Leaving it implicit means only ``show <id>``'s raw JSON can distinguish \"not yet classified\" from \"classified, not required\" \u2014 the dashboard intentionally renders both the same way (see ``_gate_suffix``, and ``test_41p``). This makes the distinction explicit in storage, for tooling/audits that read the JSON directly. There's no reliable signal in the existing schema (no per-item verdict data) to infer a *required* gate from, so this never sets ``required: true`` -- that only ever happens via a deliberate ``gate-set`` call. Dry run by default; ``--apply`` writes.",
    "handler block: Handle ``block``: add a blocker to a backlog item. ``blocker`` may be a backlog slug or a pending item's slug \u2014 a backlog item can be genuinely blocked on an external wait, not just on another backlog item. ``effective_blockers`` already treats any slug missing from the backlog index as unresolved, so a pending blocker correctly keeps the item out of READY with no further change there; this function only needed to stop rejecting it at the door. Cycle detection stays backlog-index-only deliberately: a pending item carries no ``blocked_by`` of its own, so it can never participate in a cycle. Refuses duplicates and cycle-creating blockers.",
    "handler done: Handle ``done``: mark a backlog item done.",
    "handler gate-pass: Handle ``gate-pass``: record that an item's gate criteria are satisfied. Requires an explicit per-criterion coverage payload \u2014 every numbered criterion (as ``show`` displays them) must cite either a recorded run (``run:<run_id>``: exit 0, not timed out, ``started_at`` at/after the gate's ``set_at``; one run may cover several criteria) or a non-empty manual note (``manual:<note>``) for genuinely-manual criteria such as visual checks. On success stores ``passed_at``, ``passed_via`` (``run-evidence``/``manual``/``mixed``), and the normalized coverage map on the gate, so ``show`` displays which run/note satisfied what.",
    "handler gate-set: Handle ``gate-set``: classify an item's judgment-step verification gate. Always resets ``passed_at`` to ``None`` \u2014 re-classifying a gate invalidates any prior pass (see :class:`Gate`'s docstring).",
    "handler list: Handle ``list``: print grouped, aligned backlog sections by default. Default output mirrors ``render``'s mental model: non-empty status sections (IN PROGRESS / READY / BLOCKED / IN REVIEW / DONE), with each row numbered to match ``render``'s display numbering so a number copied from ``list`` resolves identically in every mutation command via :func:`resolve_id`. The DONE section uses the same capped selection as ``render`` (:func:`_done_selection`) precisely because only those items appear in :func:`_unified_order` \u2014 an uncapped row would have no display number that any numeric-id command could resolve; older done items remain reachable by slug. ``--raw`` preserves the historical machine-readable TSV byte-for-byte. Reads items + pending + rev under :func:`backlog_lock` (same rationale as :func:`cmd_render`; pending is needed only for stable numbering).",
    "handler out-of-scope add: Handle ``out-of-scope add``: record a rejected concept.",
    "handler out-of-scope link: Handle ``out-of-scope link``: reference a backlog item from a rejected concept.",
    "handler out-of-scope list: Handle ``out-of-scope list``: list rejected concepts, newest-first.",
    "handler out-of-scope remove: Handle ``out-of-scope remove``: delete a rejected concept's record.",
    "handler out-of-scope show: Handle ``out-of-scope show``: print a rejected concept's full record.",
    "handler out-of-scope unlink: Handle ``out-of-scope unlink``: remove a backlog-item reference.",
    "handler pending add: Handle ``pending add``: track a new waiting-on-someone-else item. ``args.json`` must decode to an object with at least ``id``, ``description``, and ``kind``; see :data:`PendingItem` for the full field set.",
    "handler pending list: Handle ``pending list``: print each pending item as one JSON line.",
    "handler pending update: Handle ``pending update``: merge a JSON patch into a pending item.",
    "handler prune: Handle ``prune``: permanently remove done/resolved items older than 14 days. Backs up each data file before any deletion occurs (see :func:`_backup_before_bulk_delete`), purges inbound ``blocked_by``/ ``blocking`` references to the pruned slugs (same rationale as :func:`cmd_remove`), and only bumps the revision if something was actually removed. Computes both keep-sets and purges inbound refs entirely in memory before any file is touched, then bumps the rev and writes each changed file exactly once \u2014 previously ``items``/``pending_items`` could each be written twice in one call (once with the raw filtered set, again after ref-purging), and the rev was bumped only after all of that, so a crash anywhere in the sequence could leave any subset of the writes applied under a stale rev. Bumping first means a crash now only ever burns a rev number (see :func:`_backlog_mutation`'s docstring for the same reasoning applied there).",
    "handler ready: Handle ``ready``: print the READY bucket as a JSON array of full records. READY is *computed*, never stored \u2014 an item is ready when it is open and :func:`effective_blockers` returns nothing for it, which walks the blocker graph transitively on every call. A caller that wants that set has to either ask this script or reimplement the walk, and a second implementation would drift the moment the graph rules changed. So this exposes the bucket :func:`_render_order` already builds for the dashboard, rather than leaving each consumer to derive it. Full records, not ids: the caller this exists for (``swarm_spawn``'s scheduler, via ``pi/extensions/swarm-tool.ts``) needs ``related_files`` to tell whether two items would edit the same file, and a second round-trip per item to fetch that would be both slower and racier. Pure \u2014 same contract as :func:`cmd_render`, no writes, safe to call on a loop while workers are running.",
    "handler recap: Handle ``recap``: the only synchronous regen path (explicit user intent). Bare: prints the cache if fresh *and* its board fingerprint still matches the live board, otherwise blocks on the regen lock and regenerates (double-checking freshness after acquiring, in case an in-flight detached child just refreshed it). ``--refresh`` bypasses the freshness check entirely and always regenerates.",
    "handler reject: Handle ``reject``: send an in-review item back to in-progress with feedback.",
    "handler remove: Handle ``remove``: permanently delete one backlog item. Purges the removed slug from every surviving item's ``blocked_by`` list and every pending item's ``blocking`` list, so deleting a completed blocker doesn't retroactively flip its dependents from READY into BLOCKED via :func:`effective_blockers`'s missing-slug-is-unresolved fallback. The pending-side purge is saved explicitly here \u2014 :func:`_backlog_mutation`'s cleanup only persists ``items``, so a ``blocking``-list edit made only in memory would otherwise vanish the moment this process exits, leaving the stale slug on disk for the next command to reload.",
    "handler rename: Handle ``rename``: rename a slug and rewrite every reference to it. Rewrites references across both backlog and pending items: backlog ``blocked_by`` lists and prose fields (``summary``/``context``/ ``next_steps``), pending ``blocking`` lists and ``related_files[].note`` prose. Prose substitution uses a boundary anchored on the slug alphabet ``[a-z0-9-]`` (negative lookbehind/lookahead) \u2014 ``\\b`` would over-match because ``-`` is a non-word char, so ``\\bfoo-bar\\b`` matches the ``foo-bar`` prefix of the unrelated sibling slug ``foo-bar-baz``.",
    "handler render: Handle ``render``: print the dashboard with no other side effects. Reads items + pending + rev atomically under :func:`backlog_lock` so the printed (items, rev) pair is self-consistent \u2014 a writer committing between the item read and the rev read would otherwise pair a stale item-map with a fresh rev, defeating a downstream numeric ``--if-rev`` guard. The actual ``render`` call (and its recap dispatch check) happens after the lock is released \u2014 agents run a bare ``render`` before every numeric mutation to fetch the rev for ``--if-rev``, so this path must stay instant and never hold the lock across a possible recap-regen spawn.",
    "handler review: Handle ``review``: submit (or re-submit) an item for review. Valid from ``in-progress`` (normal submission) or from ``in-review`` itself (re-pins the content hash after a drift refusal, clearing any stale feedback, without changing status).",
    "handler run: Handle ``run``: execute a command and record it as run evidence. Executes ``args.command`` via :func:`subprocess.run` \u2014 no shell, output inherited (visible live; a 30-minute suite must not look like a hung prompt) \u2014 then appends one truthful row (exit code, duration, timeout flag) to the ``runs.jsonl`` sidecar for ``gate-pass`` to cite. Lock scope: the item-existence check and the append each take :func:`backlog_lock` briefly; the subprocess itself runs **outside** the lock \u2014 holding it across a long test run would freeze every other harness session sharing the store. A numeric ``id`` is resolved against the full pending+backlog pool (matching ``render``'s own numbering) and then checked with :func:`require_kind` \u2014 resolving against the backlog pool alone would silently misnumber every position once any pending item exists, since pending items always come first in the dashboard's numbering. Like every other numeric-id command, a stale position is caught by :func:`enforce_rev_guard` rather than silently attributing this run's evidence to the wrong item.",
    "handler runs: Handle ``runs``: list an item's recorded run evidence.",
    "handler show: Handle ``show``: print the full JSON record for one item. Reads items + pending + rev under :func:`backlog_lock` (same rationale as :func:`cmd_render`).",
    "handler start: Handle ``start``: mark a backlog item in-progress.",
    "handler unblock: Handle ``unblock``: remove a blocker from a backlog item.",
    "handler update: Handle ``update``: merge a JSON patch into a backlog item.",
    "subcommand add: append a new item (id required in JSON)",
    "subcommand approve: approve an in-review item, marking it done",
    "subcommand backfill-gate: stamp an explicit inert gate on legacy items",
    "subcommand block: add a blocker to an item",
    "subcommand done: mark item done",
    "subcommand gate-pass: record that an item's gate criteria are satisfied",
    "subcommand gate-set: classify an item's judgment-verification gate",
    "subcommand list: grouped backlog table (--raw for tab-separated output)",
    "subcommand out-of-scope add: record a rejected concept",
    "subcommand out-of-scope link: reference a backlog item from a rejected concept",
    "subcommand out-of-scope list: list rejected concepts, newest-first",
    "subcommand out-of-scope remove: delete a rejected concept's record",
    "subcommand out-of-scope show: print a rejected concept's full record",
    "subcommand out-of-scope unlink: remove a backlog-item reference from a rejected concept",
    "subcommand out-of-scope: record/browse rejected feature concepts (distinct from 'reject', which sends an in-review item back for rework)",
    "subcommand pending add: track a new pending item",
    "subcommand pending list: list pending items as JSON lines",
    "subcommand pending update: merge a JSON patch into an existing pending item",
    "subcommand pending: manage pending (waiting-on-reply) items",
    "subcommand prune: permanently remove done/resolved items older than 14 days",
    "subcommand ready: print the READY bucket as JSON (open, unblocked, full records)",
    "subcommand recap: print a friendly prose recap of recent activity",
    "subcommand reject: reject an in-review item, sending it back to in-progress",
    "subcommand remove: permanently remove one item by slug or number",
    "subcommand rename: rename slug (rewrites all references)",
    "subcommand render: render dashboard (pure \u2014 no side effects)",
    "subcommand review: submit (or re-submit) an item for review",
    "subcommand run: execute a command and record it as run evidence for an item",
    "subcommand runs: list recorded run evidence for an item",
    "subcommand show: print full JSON for an item",
    "subcommand start: mark item in-progress",
    "subcommand unblock: remove a blocker from an item",
    "subcommand update: merge JSON patch into an item"
  ],
  "gen_interfaces.py": [
    "arg (top-level) --check: \u2014 exit 3 (with a report on stderr) if a doc's shown command example no longer matches its script, else exit 1 if the file on disk is stale",
    "arg (top-level) --output: ",
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --repo-root: \u2014 repository root (default: inferred from this script's path)",
    "arg (top-level) --stdout: \u2014 print the document, write nothing",
    "arg (top-level) --update-fingerprints: \u2014 recompute and record every in-scope script's contract fingerprint in contract_fingerprints.json, accepting its current behavior as the new baseline \u2014 run this only after re-reading the --check diff, never blind",
    "arg (top-level) --verbose/-v: ",
    "exit codes: 1, 2, 3",
    "module docstring: gen_interfaces.py \u2014 regenerate INTERFACES.md mechanically from the sources."
  ],
  "grill.py": [
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --verbose/-v: ",
    "arg ask --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg ask json: ",
    "arg decide --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg decide json: ",
    "arg frontier --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg mark-pending-execution --backlog-slug: \u2014 dev_status.py item this plan belongs to, if any",
    "arg mark-pending-execution --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg new json: ",
    "arg next --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg pending-plan --consume: \u2014 clear pending_execution on the printed session (one-shot)",
    "arg plan --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg plan path: ",
    "arg render --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg revise --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg revise decision_id: ",
    "arg revise patch: ",
    "arg rm --force: \u2014 bypass the referential-integrity check (dangling depends_on allowed)",
    "arg rm --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg rm decision_id: ",
    "arg show --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg show decision_id: (nargs: ?)",
    "arg verdict --session/-s: \u2014 session slug or unique substring (default: most recent)",
    "arg verdict decision_id: ",
    "arg verdict json: ",
    "exit codes: 1",
    "handler ask: Handle ``ask``: register an open decision point.",
    "handler decide: Handle ``decide``: resolve an open decision point, or add+decide in one shot.",
    "handler frontier: Handle ``frontier``: print the batch of currently-askable open decisions. A decision is \"currently-askable\" once every id it names in ``depends_on`` is either decided or absent from the session (see :func:`frontier`) \u2014 this is what lets `grill-me`'s Q&A loop batch a deterministic, script-computed set of questions per round instead of trusting the model's own informal judgment of dependency order.",
    "handler list: Handle ``list``: print one summary line per session, tab-separated.",
    "handler mark-pending-execution: Handle ``mark-pending-execution``: flag a session's plan as clear-and-go ready. ``--backlog-slug`` records which ``dev_status.py`` item this plan belongs to, when known \u2014 e.g. `/backlog-item`'s handoff step, which already has the item's slug in scope. `pending-plan` uses it to point the resumed session at `/backlog-item <slug>` instead of the plan file directly, so the item's own state/gates aren't bypassed.",
    "handler new: Handle ``new``: create a session and print its slug on stdout.",
    "handler next: Handle ``next``: print the first frontier-ready open decision, if any.",
    "handler pending-plan: Handle ``pending-plan``: print (and optionally consume) the most recent pending-execution plan. Called from the SessionStart hook with ``--consume`` so a plan flagged via ``mark-pending-execution`` in a prior conversation surfaces automatically at the next session's start. Silent (no output) when nothing is pending, which is the common case \u2014 this must stay side-effect-free noise on every other session start. A single unreadable or non-session file in ``DATA_DIR`` must not block the scan: ``load_session`` prints its own diagnostic and exits, so that's caught per-slug and skipped here rather than propagated.",
    "handler plan: Handle ``plan``: record the path of the model-authored plan artifact.",
    "handler render: Handle ``render``: print session status as Markdown.",
    "handler revise: Handle ``revise``: amend a decision's text (resets its verdict). Rejects blanking out ``question``/``decision`` via revise (whether by explicit ``null`` or an empty string) \u2014 that would silently walk an already-decided item back toward \"open\" without going through :func:`cmd_decide`'s own validation, and without :func:`is_open` (which only checks for ``None``) ever flagging it.",
    "handler rm: Handle ``rm``: remove a decision point from a session. Refuses to remove a decision still named in another decision's ``depends_on`` (referential integrity), unless ``--force`` is passed \u2014 in which case the referencing decision's ``depends_on`` entry goes dangling on purpose, which :func:`frontier` already treats as resolved.",
    "handler show: Handle ``show``: print a session (or one decision within it) as JSON.",
    "handler verdict: Handle ``verdict``: record a verification result for a decided item.",
    "subcommand ask: register an open decision point",
    "subcommand decide: resolve an open decision point (or add+decide in one shot)",
    "subcommand frontier: print the batch of currently-askable (dependency-resolved) open decisions",
    "subcommand list: list sessions",
    "subcommand mark-pending-execution: flag a session's plan as ready for clear-and-go resume",
    "subcommand new: create a session",
    "subcommand next: print the first frontier-ready open decision point",
    "subcommand pending-plan: print (and optionally consume) the most recent pending-execution plan",
    "subcommand plan: record the path of the model-authored plan artifact",
    "subcommand render: print session status as markdown",
    "subcommand revise: amend a decision (resets its verdict)",
    "subcommand rm: remove a decision point from a session",
    "subcommand show: print session (or one decision) as JSON",
    "subcommand verdict: record a verification verdict"
  ],
  "herdr_delegate.py": [
    "arg launch --cwd: \u2014 working directory",
    "arg launch --model: \u2014 model passed through to pi after a bare --",
    "arg launch --prefix: \u2014 queue scope, required with --swarm",
    "arg launch --slug: \u2014 single item for one unattended worker",
    "arg launch --swarm: \u2014 fan out across N workers",
    "exit codes: 1",
    "handler launch: Create a tab, start pi in it, and hand it its prompt.",
    "handler plan: Print the READY queue grouped by prefix, as JSON. No side effects.",
    "subcommand launch: start a pi worker or orchestrator",
    "subcommand plan: READY queue grouped by prefix, as JSON"
  ],
  "second_opinion.py": [
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --verbose/-v: ",
    "arg review --backend: \u2014 force this backend instead of priority-order fallback (choices computed at runtime)",
    "arg review --focus-file: \u2014 path to a file of plan-specific risk hints, appended to the critique prompt as areas to scrutinize (supplements, not replaces, the generic adversarial mandate)",
    "arg review --model-index: \u2014 0-based index into the backend model pool (SECOND_OPINION_{AGY,PI,OPENCODE,COPILOT}_MODEL_POOL) for this call -- round 1 of a rotation is index 0, round 2 is index 1, etc. Supported for agy/pi/opencode/copilot; an explicit index selects the pool even when a single-model override is set, and is a hard error if the pool is unset/empty or the index is out of range (was previously a silent no-op/fallback).",
    "arg review plan: ",
    "exit codes: 1",
    "handler detect: Handle ``detect``: print backend presence AND contract eligibility. Presence alone was misleading: an installed backend that cannot meet the isolation contract is never used, so reporting it as simply available invited the discovery to happen at the moment a critique was wanted rather than before. Each entry carries ``present``, ``eligible`` and, when ineligible, the ``reason``. The legacy boolean shape is preserved under each backend's ``present`` key so existing callers keep a one-line migration.",
    "handler review: Handle ``review``: get one critique from the priority-selected backend. Tries each candidate backend in priority order (or just the forced ``--backend``, if given) until one succeeds; prints the first successful critique and returns. Exits nonzero only if every candidate fails.",
    "subcommand detect: list available backends as JSON",
    "subcommand review: get one critique from the priority-selected backend"
  ],
  "standup.py": [
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --verbose/-v: ",
    "arg fetch --date: \u2014 override reference date (YYYY-MM-DD) \u2014 for re-running after a gap (holiday, PTO) where the default last-working-day boundary would miss it",
    "exit codes: 1",
    "handler fetch: ",
    "subcommand fetch: gather all sources as JSON"
  ],
  "to_tickets_runner.py": [
    "arg run batch_file: \u2014 path to the batch JSON file",
    "exit codes: 1",
    "handler run: ",
    "subcommand run: create every ticket in a batch file"
  ],
  "vitals_promotion.py": [
    "arg (top-level) --apply: \u2014 write vitals/needs-review files (default: dry-run, prints only)",
    "arg (top-level) --data-dir: \u2014 grill session data directory (default: ~/.claude/data/grill)",
    "arg (top-level) --needs-review-summary: \u2014 print a one-line summary of the latest needs-review file and exit",
    "arg (top-level) --quiet/-q: ",
    "arg (top-level) --verbose/-v: ",
    "exit codes: ",
    "module docstring: vitals-promotion.py \u2014 mechanical vitals-promotion pass over grill session data."
  ]
}
